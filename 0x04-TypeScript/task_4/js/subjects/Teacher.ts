namespace Subjects {
  // Interface defining the structure of a Teacher
  export interface Teacher {
    firstName: string; // The teacher's first name
    lastName: string;  // The teacher's last name
  }
}
