
// Defining a type alias for RowID, which represents a number
export type RowID = number;

// Defining an interface RowElement to describe the structure of a row object
export interface RowElement {
  firstName: string;  
  lastName: string;   
  age?: number;       
}
