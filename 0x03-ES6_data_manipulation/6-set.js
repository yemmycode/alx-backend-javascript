/**
 * Generates a new Set using the elements from the provided array.
 *
 * @param {Array} array - The source array for creating the Set
 * @return {Set} A newly created Set with the elements from the array
 */
export default function setFromArray(array) {
  return new Set(array);
}
